# Backend Environment Variables
# Copy this to .env and fill in values

# Database (PostgreSQL)
DATABASE_URL=postgresql://user:password@localhost:5432/linera_arcade

# Server
PORT=3001
NODE_ENV=development

# CORS - Frontend URLs (comma-separated for multiple)
CORS_ORIGINS=http://localhost:3005,http://localhost:5173,https://your-app.vercel.app

# API Key for write operations (generate a random string)
API_SECRET_KEY=your-secret-api-key-here
